import sys.args

int main() {
    long n = args.count()
    if n >= 1 {
        print("count ok")
    }
    str prog = args.at(0)
    if len(prog) > 0 {
        print("at ok")
    }
    list all = args.all()
    if len(all) >= 1 {
        print("all ok")
    }
    print("args ok")
    return 0
}

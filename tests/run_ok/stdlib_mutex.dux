import thread.mutex

int main() {
    ptr m = mutex.create()
    mutex.lock(m)
    mutex.unlock(m)
    bool try_ok = mutex.trylock(m)
    if try_ok {
        print("trylock ok")
        mutex.unlock(m)
    }
    mutex.free(m)
    print("mutex ok")
    return 0
}

# switch / case / default — case uses colon, not braces
void main() {
    x = 2
    switch x {
        case 1:
            println("one")
        case 2:
            println("two")
        default:
            println("other")
    }
}

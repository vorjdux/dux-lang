# Lambda closures using function values
# Dux lambdas: assigned as function expressions but resolved as ident
void applyTwice(int x) {
    y = x * 2
    println(y)
}

void main() {
    applyTwice(5)
    applyTwice(add(3, 4))
}

int add(int a, int b) {
    return a + b
}

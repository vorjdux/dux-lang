class Counter {
    static int count = 0
    static const int MAX = 1000

    static void increment() {
        Counter.count += 1
    }

    static int value() {
        return Counter.count
    }
}

void main() {
    Counter.increment()
    int v = Counter.value()
    int m = Counter.MAX
}
